==================
struct declaration
==================

struct VertexOutput {
  @builtin(position) position: vec4<f32>,
  @location(0) color: vec4<f32>,
}

----

(source_file)

==================
global variable with attributes
==================

@group(0) @binding(0) var<uniform> viewProjection: mat4x4<f32>;

----

(source_file)

==================
vertex shader function
==================

@vertex
fn vs_main(@location(0) pos: vec3<f32>) -> @builtin(position) vec4<f32> {
  return vec4<f32>(pos, 1.0);
}

----

(source_file)

==================
fragment shader function
==================

@fragment
fn fs_main() -> @location(0) vec4<f32> {
  return vec4<f32>(1.0, 0.0, 0.0, 1.0);
}

----

(source_file)

==================
let and var statements
==================

fn compute() {
  let a: f32 = 1.0;
  var b: f32 = 2.0;
  b = a + b;
}

----

(source_file)

==================
if else statement
==================

fn clamp_val(x: f32) -> f32 {
  if (x < 0.0) {
    return 0.0;
  } else {
    return x;
  }
}

----

(source_file)

==================
for loop
==================

fn loop_sum() -> i32 {
  var total: i32 = 0;
  for (var i: i32 = 0; i < 10; i = i + 1) {
    total = total + i;
  }
  return total;
}

----

(source_file)

==================
type alias and const
==================

type Color = vec4<f32>;
let PI: f32 = 3.14159;

----

(source_file)
