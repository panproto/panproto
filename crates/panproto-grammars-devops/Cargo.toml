[package]
name = "panproto-grammars-devops"
description = "Companion grammar package: devops languages for the panproto Python wheel"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lib]
# Globally-unique cdylib name to avoid `PyInit_<name>` symbol
# collisions across companions in the same Python process.
name = "panproto_grammars_devops_impl"
crate-type = ["cdylib"]

[dependencies]
# `default-features = false` lets this companion ship only the
# `group-devops` grammars, not the workspace-default `group-core`.
# Workspace inheritance is bypassed because the workspace dep
# doesn't pin default-features.
panproto-grammars = { version = "=0.47.3", path = "../panproto-grammars", default-features = false, features = ["group-devops"] }
pyo3 = { workspace = true }
tree-sitter = { workspace = true }

[lints]
workspace = true
